{
  "name": "estado_respuesta",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "Estado": {
        "type": "string",
        "enum": ["Abierto", "Cerrado"]
      },
      "Respuesta": {
        "type": "string"
      }
    },
    "required": ["Estado", "Respuesta"],
    "additionalProperties": false
  }
}
